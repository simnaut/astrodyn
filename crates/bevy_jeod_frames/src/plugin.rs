use bevy::prelude::*;

pub struct JeodFramesPlugin;

impl Plugin for JeodFramesPlugin {
    fn build(&self, _app: &mut App) {
        // Phase 2: will add frame tree propagation and derived state systems.
    }
}
